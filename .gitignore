# 构建产物：模型、包内 Python 和归档都不进 git。
# 归档走 GitHub Release（Release 资源不计 LFS 配额，也不需要 git clone 拉下来）。
# 注意是 `**/`：引擎在 engines/<id>/ 下面，各引擎各有自己的 build/ 与 dist/。
**/build/
**/dist/*.zip
**/target/
**/__pycache__/
**/*.pyc
# 单平台构建记录和 repositories/default.jsonl 很小，允许提交。
!**/dist/catalog-entry-*.json

# 模型权重只在构建机上保存；归档上传 Release，不能提交模型或本地开发包。
arale_onnx_v1/models/
arale_onnx_v1/runtime/
